# AGENTS.md

> Auto-generated by `plan-workflow` on <YYYY-MM-DD>. Please review and correct.
> This file is the source of truth for AI agents working in this repo. Keep it
> short, factual, and current.

## Project overview

- One-paragraph description of what this project is and does.

## Stack

- Language(s) and version(s):
- Runtime / platform:
- Framework(s):
- Package / dependency manager:
- Build tool:
- Test framework:
- Linter / formatter:
- Database / data store(s):
- Other infrastructure (queues, caches, search, etc.):

## Common commands

Fill in the actual commands for this project. Skip rows that don't apply.

| Task | Command |
|------|---------|
| Install dependencies | |
| Run dev server | |
| Run all tests | |
| Run a single test | |
| Lint | |
| Format | |
| Build | |
| Type-check | |
| Apply migrations | |
| Create a migration | |
| Regenerate code (mocks, ORM, protobuf, OpenAPI client, etc.) | |

## Architecture / layout

Describe the project's actual layering and where things live. Use real
directory names. Example shape (adapt freely):

- App entrypoint(s):
- Domain / business logic:
- Use case / service / application layer:
- Transport / API layer (HTTP, RPC, GraphQL, etc.):
- Persistence / data access layer:
- Background jobs / workers:
- Shared types / contracts / ports:
- Generated code (and what generates it):
- Tests (location and convention):
- Configuration (files, env vars, feature flags):

## Shared choke points

Files that many features touch in passing and that need extra care under
parallel agent writes. List the concrete files for THIS project:

- Route / endpoint / resolver registry:
- App bootstrap / DI / composition root:
- Shared interfaces / contracts:
- Migration directory / sequence:
- Generated code that is regenerated wholesale:
- Package manifest(s) and lockfile(s):
- Build / framework / CI config:

## Conventions

- Branch / commit / PR style:
- Code style rules not enforced by the formatter:
- Naming conventions:
- Error handling pattern:
- Testing pattern (what is mocked vs. live):

## Gotchas

- Anything non-obvious that has bitten people before: codegen markers, generated
  blocks, single-package mocks, framework quirks, env-var precedence, etc.

## Available agent skills

- List any project-specific skills present in `.agents/skills/` and when to use
  them. If none, write \"None — this project relies on the generic skills only.\"
